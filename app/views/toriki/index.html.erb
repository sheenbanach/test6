<h1>Listing books</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Name</th>
  </tr>
 
  <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.name %></td>
    </tr>
  <% end %>
</table>
  
